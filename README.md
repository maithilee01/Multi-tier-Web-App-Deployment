# Multi-tier-Web-App-Deployment
This project demonstrates a complete multi-tier web application deployed on AWS. It includes a frontend hosted on EC2, a backend API built using Flask, and a MySQL database hosted on Amazon RDS.  The application fetches data from the database through the backend and displays it on the frontend, showcasing real-world cloud architecture.
